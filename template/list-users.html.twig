{% extends 'base.html.twig' %}
{% block title %}Wybór gracza{% endblock %}
{% block body %}
    <div class="flex-col-md-6 text-center">
        <div class="title_fixed">
            <p class="txt-white">Witaj w webowym panelu statyk KentoRankMe stworzonym przez KoderZ.pl <br />
                Kolejnym krokiem jest znalezienie gracza, <br />którego statystyki Cię interesują na liście <br />lub przy użyciu poniższego formularza wyszukiwania:
            </p>
            <form  action="" method="post">
                <input type="text" class="form-control" placeholder="Szukaj nicku gracza..." name="nickname" required />
                <button type="submit" class="btn-white ">Szukaj</button>
            </form>
            <hr />
            <h3 class="txt-white">Wybór gracza</h3>
        </div>

    </div>
    <div class="flex-col-md-6 text-center">
        <ul class="server_list">
            {% for user in users %}

                <li class="server_box shadow-1">
                    <a href="/?server={{ server }}&action=user-details&id={{ user.id }}" >
                        <div class="flex-row">
                            <div class="flex-col-md-9">
                                <p>{{ user.name }}</p>
                            </div>
                            <div class="flex-col-md-3">
                                <p>{{ user.points }} pkt.</p>
                            </div>
                        </div>
                    </a>
                </li>

            {% endfor %}
        </ul>
    </div>
{% endblock %}